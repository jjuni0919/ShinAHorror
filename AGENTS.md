# 프로젝트 작업 지침

- 답변, 작업 진행 설명, 코드 주석, 로그, 에디터 표시 문구는 한국어를 기본으로 한다.
- 기존 프로젝트의 구조, 네이밍, 포맷, 공개 API를 우선하고 변경 범위를 최소화한다.
- 구현 전에 책임 위치, 재사용 가능한 기존 코드, 최소 변경 가능성, 추상화 필요성을 확인한다.
- 현재 요구에 필요하지 않은 범용화, 확장 지점, 인터페이스, 의존성 주입을 추가하지 않는다.
- 작은 기능을 위해 Manager, Service, Helper, Util 클래스를 새로 만들지 않는다.
- 한 번만 쓰이는 로직을 불필요하게 분리하지 않되, 여러 책임을 가진 함수는 적절히 나눈다.
- 이름만으로 의도를 알 수 있게 작성하고, 코드의 동작을 반복 설명하는 주석은 남기지 않는다.
- 주석은 구현 이유나 제약을 설명해야 할 때만 작성한다.
- TODO, 임시 구현, 목업, 사용되지 않는 확장 포인트를 임의로 남기지 않는다.
- null, 잘못된 입력, 실패 상황, 경쟁 상태를 고려하되 과도한 방어 코드와 예외 숨기기를 피한다.
- 기존 유틸리티, 타입, 인터페이스를 먼저 재사용하고 매직 넘버와 의미 없는 문자열의 중복을 피한다.
- 수정할 필요가 없는 파일과 코드는 건드리지 않는다.
- 구현 후에는 변경한 파일, 변경 이유, 잠재적인 엣지 케이스, 테스트 항목만 정리한다.

## Unity 작업 제한

- Unity AI Assistant 및 Unity AI Generators를 사용하지 않는다.
- `generators.ai.unity.com`에 접근하거나 해당 서비스를 호출하지 않는다.
- `com.unity.ai.assistant`, `com.unity.ai.generators` 및 기타 Unity 생성형 AI 관련 패키지를 설치하거나 사용하지 않는다.
- 모든 코드 생성 및 수정은 Codex가 직접 수행한다.
- Unity 관련 참고 자료는 Unity API와 Unity 공식 문서만 사용한다.
- 사용자가 명시적으로 요청하지 않는 한 외부 생성형 AI 서비스를 사용하지 않는다.
